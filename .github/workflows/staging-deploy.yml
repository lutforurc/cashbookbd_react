name: Staging React-Application CI/CD Pipeline

on:
  push:
    branches: ["react-staging"]
    tags:
      - "*"
  pull_request:
    branches: ["react-staging"]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: staging-deploy
  cancel-in-progress: true

jobs:
  sanity-check:
    name: Sanity trigger check
    runs-on: ubuntu-latest
    steps:
      - run: echo "✅ Workflow triggered on $GITHUB_REF ($GITHUB_SHA)"

  build-and-deploy:
    name: Build and Deploy React App
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: [sanity-check]
    env:
      NODE_ENV: production
      REMOTE_DIR: ${{ secrets.REACT_STAGING_REMOTE_DIR || '/home/react_staging/htdocs/react.staging.cashbookbd.com/' }}
      DEPLOY_URL: ${{ secrets.REACT_STAGING_DEPLOY_URL || 'https://react.staging.cashbookbd.com' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Clear and verify npm cache
        run: |
          npm cache clean --force
          npm cache verify

      - name: Check Node.js and npm versions
        run: |
          node -v
          npm -v

      - name: Validate secrets
        run: |
          set -Eeuo pipefail
          test -n "${{ secrets.REACT_STAGING_SSH_PRIVATE_KEY }}" || (echo "❌ REACT_STAGING_SSH_PRIVATE_KEY is empty" && exit 1)
          test -n "${{ secrets.REACT_STAGING_USERNAME }}" || (echo "❌ REACT_STAGING_USERNAME is empty" && exit 1)
          test -n "${{ secrets.REACT_STAGING_HOST }}" || (echo "❌ REACT_STAGING_HOST is empty" && exit 1)
          test -n "${{ secrets.REACT_STAGING_REMOTE_DIR }}" || (echo "❌ REACT_STAGING_REMOTE_DIR is empty" && exit 1)
          test -n "${{ secrets.REACT_STAGING_DEPLOY_URL }}" || (echo "❌ REACT_STAGING_DEPLOY_URL is empty" && exit 1)

      - name: Debug package.json and dependencies
        run: |
          set -Eeuo pipefail
          echo "📜 package.json contents:"
          cat package.json
          echo "📜 package-lock.json exists:"
          ls -la package-lock.json || echo "❌ No package-lock.json found"
          echo "📜 node_modules/.bin contents (before install):"
          ls -la node_modules/.bin || echo "No node_modules/.bin directory yet"

      - name: Install dependencies
        run: |
          set -Eeuo pipefail
          echo "📦 Installing dependencies with npm install"
          npm install --loglevel verbose --registry https://registry.npmjs.org 2>&1 | tee npm-install.log
          echo "📜 npm install log:"
          cat npm-install.log

      - name: Verify vite and yaml installation
        run: |
          set -Eeuo pipefail
          if [ -f ./node_modules/.bin/vite ]; then
            echo "✅ vite binary found"
          else
            echo "❌ vite binary not found"
            exit 1
          fi
          if [ -d ./node_modules/yaml ]; then
            echo "✅ yaml package found"
          else
            echo "❌ yaml package not found"
            exit 1
          fi

      - name: Debug installed dependencies
        run: |
          set -Eeuo pipefail
          echo "📜 Installed dependencies:"
          npm list --depth=0 || echo "npm list failed"
          echo "📜 node_modules/.bin contents (after install):"
          ls -la node_modules/.bin

      - name: Build React app (auto-detect outdir)
        env:
          CI: true
        run: |
          set -Eeuo pipefail
          ./node_modules/.bin/vite build
          OUTDIR=""
          if [ -d dist ]; then
            OUTDIR="dist"
          elif [ -d build ]; then
            OUTDIR="build"
          fi
          test -n "$OUTDIR" || (echo "❌ No dist/ or build/ found. Check your bundler output." && exit 1)
          echo "OUTDIR=$OUTDIR" >> "$GITHUB_ENV"

      - name: Upload build artifact (for debugging/rollback)
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.OUTDIR }}-${{ github.sha }}
          path: ${{ env.OUTDIR }}
          if-no-files-found: error
          retention-days: 7

      - name: Prepare SSH
        env:
          SSH_PRIVATE_KEY: ${{ secrets.REACT_STAGING_SSH_PRIVATE_KEY }}
          SSH_USER: ${{ secrets.REACT_STAGING_USERNAME }}
          SSH_HOST: ${{ secrets.REACT_STAGING_HOST }}
          SSH_PORT: ${{ secrets.REACT_STAGING_PORT }}
        run: |
          set -Eeuo pipefail
          mkdir -p ~/.ssh
          printf '%s\n' "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          echo "⚙️ Fetching host key using ssh-keyscan..."
          ssh-keyscan -p "${SSH_PORT:-22}" -H "$SSH_HOST" \
            | sed "s/^$SSH_HOST/[${SSH_HOST}]:${SSH_PORT:-22}/" >> ~/.ssh/known_hosts
          cat > ~/.ssh/config <<EOF
          Host staging-host
            HostName $SSH_HOST
            Port ${SSH_PORT:-22}
            User $SSH_USER
            StrictHostKeyChecking yes
            UserKnownHostsFile ~/.ssh/known_hosts
          EOF

      - name: Debug SSH connection (optional)
        continue-on-error: true
        run: |
          ssh -vvv -p "${{ secrets.REACT_STAGING_PORT }}" "${{ secrets.REACT_STAGING_USERNAME }}@${{ secrets.REACT_STAGING_HOST }}" "echo '✅ Connected:' && whoami && hostname"

      - name: Deploy build via rsync
        env:
          SSH_USER: ${{ secrets.REACT_STAGING_USERNAME }}
          SSH_HOST: ${{ secrets.REACT_STAGING_HOST }}
          SSH_PORT: ${{ secrets.REACT_STAGING_PORT }}
        run: |
          set -Eeuo pipefail
          test -n "${OUTDIR:-}" || (echo "OUTDIR not set" && exit 1)
          echo "📦 Local build contents:"
          ls -la "$OUTDIR"/
          ssh -p "${SSH_PORT:-22}" "$SSH_USER@$SSH_HOST" "mkdir -p '$REMOTE_DIR'"
          rsync -avz \
            --delete-delay \
            --numeric-ids \
            --chmod=D755,F644 \
            --exclude='.DS_Store' \
            -e "ssh -p ${SSH_PORT:-22}" \
            "$OUTDIR"/ "$SSH_USER@$SSH_HOST:$REMOTE_DIR/"

      - name: Fix permissions on server
        env:
          SSH_USER: ${{ secrets.REACT_STAGING_USERNAME }}
          SSH_HOST: ${{ secrets.REACT_STAGING_HOST }}
          SSH_PORT: ${{ secrets.REACT_STAGING_PORT }}
        run: |
          set -Eeuo pipefail
          ssh -p "${SSH_PORT:-22}" "$SSH_USER@$SSH_HOST" "
            sudo chown -R www-data:www-data '$REMOTE_DIR' && \
            find '$REMOTE_DIR' -type d -exec chmod 755 {} \; && \
            find '$REMOTE_DIR' -type f -exec chmod 644 {} \;
          " || { echo "❌ Failed to fix permissions"; exit 1; }

      - name: Reload web server on VPS
        env:
          SSH_USER: ${{ secrets.REACT_STAGING_USERNAME }}
          SSH_HOST: ${{ secrets.REACT_STAGING_HOST }}
          SSH_PORT: ${{ secrets.REACT_STAGING_PORT }}
        run: |
          set -Eeuo pipefail
          ssh -p "${SSH_PORT:-22}" "$SSH_USER@$SSH_HOST" \
            "sudo systemctl reload nginx || sudo systemctl restart nginx"

      - name: Smoke test (HTTP 200/301/302 expected)
        run: |
          set -Eeuo pipefail
          curl -I --max-time 15 "$DEPLOY_URL" | tee /tmp/headers.txt
          grep -E '^HTTP/.* (200|301|302) ' /tmp/headers.txt >/dev/null || {
            echo '❌ Smoke test failed'; exit 1;
          }